# Weather Plugin Example

This directory shows the expected shape of a plugin package:

```text
weather/
  plugin.json
  weather.py
  README.md
```

`plugin.json` declares metadata, runtime permissions, the Python entrypoint,
and optional configuration schema.
